# Todo

# Debug
- Icons are swapped by 8 bits groups -> Write copy function that does the swap ?

# Add
- Add functions to handle the whole FSM states and commands :)
	- DCONF --> Parsing error + cannot debug on linux (USB won't enumerate)
	- UICON
