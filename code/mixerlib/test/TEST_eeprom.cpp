#include "gtest/gtest.h"
#include "eeprom/eeprom.h"

extern struct EEPROM_Header header;
extern struct EEPROM_hash hashs;

/**
 * ===============================================================
 * FIRST : CORRECT PARSER AND BUILDERS
 * ===============================================================
 */

TEST(EEPROM, CorrectParser)
{
	uint8_t buf[128] = {0xbe, 0xef,
						'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
						0x0F, 0xFF,
						0x00, 0xFF,
						0x00, 0x0F,
						0x00, 0x00,
						0x0F, 0x00,
						0x0F, 0xAA,
						64,
						0xA0,
						0xC0,
						0xE0,
						0,
						96,
						0xFF, 0xFF, 0xFF, 0xFF,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
						0x42, 0x13, 0x5b, 0xb2,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0x4a, 0x14, 0x1c, 0x65};

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, 0);

	// Preambule
	ASSERT_EQ(header.preambule, 0xbeef);

	// SN
	for (int k = 0; k < 8; k++)
	{
		ASSERT_EQ(header.SN[k], 'A');
	}

	// Offset
	EXPECT_NEAR(header.offset, 3.3f, 0.001);
	float res1[] = {0.205f, 0.012f, 0.00f, 3.094f, 3.231};
	for (int k = 0; k < 5; k++)
	{
		EXPECT_NEAR(header.chan_offsets[k], res1[k], 0.001);
	}

	EXPECT_NEAR(header.gain, 1.0f, 0.001);
	float res2[] = {-1.5f, -1.0f, -0.5f, 0.0f, 1.5f};
	for (int k = 0; k < 5; k++)
	{
		EXPECT_NEAR(header.chan_gain[k], res2[k], 0.001);
	}

	for (int k = 0; k < 5; k++)
	{
		ASSERT_EQ(header.default_apps[k], 0xdead);
	}

	for (int k = 0; k < 15; k++)
	{
		ASSERT_EQ(hashs.Icons[k].hash, 0xdead);
		ASSERT_EQ(hashs.Icons[k].address, 0xbeef);
	}
}

TEST(EEPROM, CorrectBuiler)
{
	// Set the values
	header.preambule = 0xbeef;
	for (int k = 0; k < 8; k++)
	{
		header.SN[k] = 'A';
	}

	header.offset = 3.3f;
	header.chan_offsets[0] = 0.205f;
	header.chan_offsets[1] = 0.012f;
	header.chan_offsets[2] = 0.00f;
	header.chan_offsets[3] = 3.094f;
	header.chan_offsets[4] = 3.231f;

	header.gain = 1.0f;

	header.chan_gain[0] = -1.5f;
	header.chan_gain[1] = -1.0f;
	header.chan_gain[2] = -0.5f;
	header.chan_gain[3] = 0.0f;
	header.chan_gain[4] = 1.5f;

	for (int k = 0; k < 5; k++)
	{
		header.default_apps[k] = 0xdead;
	}

	for (int k = 0; k < 15; k++)
	{
		hashs.Icons[k].hash = 0xdead;
		hashs.Icons[k].address = 0xbeef;
	}

	// allocate buf
	uint8_t buf[128] = {0};
	uint8_t ref[128] = {0xbe, 0xef,
							'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
							0x0F, 0xFF,
							0x00, 0xFF,
							0x00, 0x0F,
							0x00, 0x00,
							0x0F, 0x00,
							0x0F, 0xAA,
							64,
							0xA0,
							0xC0,
							0xE0,
							0,
							96,
							0xFF, 0xFF, 0xFF, 0xFF,
							0xde, 0xad,
							0xde, 0xad,
							0xde, 0xad,
							0xde, 0xad,
							0xde, 0xad,
							0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
							0x42, 0x13, 0x5b, 0xb2,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0xde, 0xad, 0xbe, 0xef,
							0x4a, 0x14, 0x1c, 0x65};

	// Build the output
	int retval = build_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, 0);

	for (int k = 0; k < 128; k++)
	{
		ASSERT_EQ(buf[k], ref[k]);
	}
}

/**
 * ===============================================================
 * SECOND : ERRORS TRIGGERING
 * ===============================================================
 */

TEST(EEPROM, InvalidPreambule)
{
	uint8_t buf[128] = {0xbf, 0xef,
						'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
						0x0F, 0xFF,
						0x00, 0xFF,
						0x00, 0x0F,
						0x00, 0x00,
						0x0F, 0x00,
						0x0F, 0xAA,
						64,
						0xA0,
						0xC0,
						0xE0,
						0,
						96,
						0xFF, 0xFF, 0xFF, 0xFF,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
						0x42, 0x13, 0x5b, 0xb2,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0x4a, 0x14, 0x1c, 0x65};

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -2);
}

TEST(EEPROM, InvalidCRC1)
{
	uint8_t buf[128] = {0xbe, 0xef,
						'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
						0x0F, 0xFF,
						0x00, 0xFF,
						0x00, 0x0F,
						0x00, 0x00,
						0x0F, 0x00,
						0x0F, 0xAA,
						64,
						0xA0,
						0xC0,
						0xE0,
						0,
						96,
						0xFF, 0xFF, 0xFF, 0xFF,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
						0x42, 0x14, 0x5b, 0xb2,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0x4a, 0x14, 0x1c, 0x65};

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -3);
}

TEST(EEPROM, InvalidCRC2)
{
	uint8_t buf[128] = {0xbe, 0xef,
						'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
						0x0F, 0xFF,
						0x00, 0xFF,
						0x00, 0x0F,
						0x00, 0x00,
						0x0F, 0x00,
						0x0F, 0xAA,
						64,
						0xA0,
						0xC0,
						0xE0,
						0,
						96,
						0xFF, 0xFF, 0xFF, 0xFF,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
						0x42, 0x13, 0x5b, 0xb2,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0x4a, 0x14, 0x1d, 0x65};

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -3);
}

TEST(EEPROM, InvalidOffsetCode)
{
	uint8_t buf[128] = {0xbe, 0xef,
						'A', 'A', 'A', 'A', 'A', 'A', 'A', 'A',
						0xFF, 0xFF,
						0x00, 0xFF,
						0x00, 0x0F,
						0x00, 0x00,
						0x0F, 0x00,
						0x0F, 0xAA,
						64,
						0xA0,
						0xC0,
						0xE0,
						0,
						96,
						0xFF, 0xFF, 0xFF, 0xFF,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xde, 0xad,
						0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF,
						0x22, 0xc0, 0xd0, 0x88,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0xde, 0xad, 0xbe, 0xef,
						0x4a, 0x14, 0x1c, 0x65};

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -4);
}

TEST(EEPROM, InvalidOffsetsValues)
{
	// Set the values
	header.preambule = 0xbeef;
	for (int k = 0; k < 8; k++)
	{
		header.SN[k] = 'A';
	}

	header.offset = 3.3f;
	header.chan_offsets[0] = 0.205f;
	header.chan_offsets[1] = 0.012f;
	header.chan_offsets[2] = 0.00f;
	header.chan_offsets[3] = 6.9f;
	header.chan_offsets[4] = 3.231f;

	header.gain = 1.0f;

	header.chan_gain[0] = -1.5f;
	header.chan_gain[1] = -1.0f;
	header.chan_gain[2] = -0.5f;
	header.chan_gain[3] = 0.0f;
	header.chan_gain[4] = 1.5f;

	for (int k = 0; k < 5; k++)
	{
		header.default_apps[k] = 0xdead;
	}

	for (int k = 0; k < 15; k++)
	{
		hashs.Icons[k].hash = 0xdead;
		hashs.Icons[k].address = 0xbeef;
	}

	// allocate buf
	uint8_t buf[128] = {0};

	// Build the output
	int retval = build_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -2);
}

TEST(EEPROM, InvalidGainValues)
{
	// Set the values
	header.preambule = 0xbeef;
	for (int k = 0; k < 8; k++)
	{
		header.SN[k] = 'A';
	}

	header.offset = 3.3f;
	header.chan_offsets[0] = 0.205f;
	header.chan_offsets[1] = 0.012f;
	header.chan_offsets[2] = 0.00f;
	header.chan_offsets[3] = 0.00f;
	header.chan_offsets[4] = 3.231f;

	header.gain = 1.0f;

	header.chan_gain[0] = -1.5f;
	header.chan_gain[1] = -1.0f;
	header.chan_gain[2] = -0.5f;
	header.chan_gain[3] = 0.0f;
	header.chan_gain[4] = 5.7f;

	for (int k = 0; k < 5; k++)
	{
		header.default_apps[k] = 0xdead;
	}

	for (int k = 0; k < 15; k++)
	{
		hashs.Icons[k].hash = 0xdead;
		hashs.Icons[k].address = 0xbeef;
	}

	// allocate buf
	uint8_t buf[128] = {0};

	// Build the output
	int retval = build_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -3);
}

/**
 * ===============================================================
 * THIRD : Invalid Buffers
 * ===============================================================
 */

TEST(EEPROM, ParserInvalidBuffer)
{
	uint8_t *buf = 0;

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -1);
}

TEST(EEPROM, BuilderInvalidBuffer)
{
	uint8_t *buf = 0;

	// Parse buffer
	int retval = parse_raw_eeprom_header(buf);

	// Comparisons
	ASSERT_EQ(retval, -1);
}

TEST(EEPROM, AccessTests)
{

	// Set some addresses
	for (int k = 0; k < 15; k++)
	{
		hashs.Icons[k].hash = (0xaaa0 + k);
		hashs.Icons[k].address = ((k * 128) + 128);
	}

	// Comparisons
	for (int k = 0; k < 15; k++)
	{
		ASSERT_EQ(get_eeprom_address_from_hash(0xaaa0 + k), ((k * 128) + 128));
	}
	for (int k = 0; k < 15; k++)
	{
		uint32_t tmp = 0x555FFFFF - k;
		ASSERT_EQ(get_eeprom_address_from_app(tmp), ((k * 128) + 128));
	}
}


TEST(EEPROM, FreeAndAllocate)
{

	// Set some addresses
	for (int k = 0; k < 15; k++)
	{
		hashs.Icons[k].hash = (0xaaa0 + k);
		hashs.Icons[k].address = ((k * 128) + 128);
	}

	// Comparisons
	for (int k = 0; k < 15; k++)
	{
		ASSERT_EQ(free_eeprom_address(0xaaa0 + k), 0);
	}

	// Verify correct addressing
	for (int k = 0; k < 15; k++)
	{
		ASSERT_EQ(hashs.Icons[k].hash, 0);
	}

	// Fetch new addresses
	for (int k = 0; k < 6; k++)
	{
		ASSERT_EQ(get_new_eeprom_address_from_hash(0xaaa0), ((k * 128) + 128));
	}
	// Fetch new addresses
	for (int k = 6; k < 14; k++)
	{
		ASSERT_EQ(get_new_eeprom_address_from_app(0x555FFFFF), ((k * 128) + 128));
	}

	// Ensure hashes where correctly storeds
	for (int k = 0; k < 14; k ++)
	{
		ASSERT_EQ(hashs.Icons[k].hash, 0xaaa0);
	}
}




